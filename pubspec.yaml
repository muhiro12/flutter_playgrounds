name: flutter_playgrounds
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^1.0.0-dev.7
  openapi:
    path: openapi
dev_dependencies:
  flutter_test:
    sdk: flutter
  import_sorter: ^4.6.0
flutter:
  uses-material-design: true